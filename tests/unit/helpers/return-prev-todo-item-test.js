import { returnPrevTodoItem } from 'ember-quickstart/helpers/return-prev-todo-item';
import { module, test } from 'qunit';

module('Unit | Helper | return prev todo item');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = returnPrevTodoItem([42]);
  assert.ok(result);
});

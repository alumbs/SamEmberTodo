import { isTodoIndexValid } from 'ember-quickstart/helpers/is-todo-index-valid';
import { module, test } from 'qunit';

module('Unit | Helper | is todo index valid');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isTodoIndexValid([42]);
  assert.ok(result);
});
